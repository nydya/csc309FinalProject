// TODO: show next step whenever user clicks confirm

// page should be initially have only step 1 showing with no error messages
// error messages should appear if user does something stupid

// once create your trip button is pressed without error we should place something into the database

/*
put trip under user as a blank momento
class trip {
	constructor(name, startDate, endDate, destinations, stayLength) {
		this.name = name;
		this.startDate = startDate;
		this.endDate = endDate;
		this.destinations = destinations;
		this.stayLength = stayLength;
	}
}

const datetime = require('date-and-time')

const submitDate = document.querySelector('#pnTpS');
bookAddForm.addEventListener('submit', populateFields);

function populateFields(e) {
	e.preventDefault();
	
	const startDate = document.querySelector('#stDt').value
	const endDate = document.querySelector('#stDt').value

	let checkboxes = document.getElementsByName('Destination');
	const duration = document.querySelectorAll('.styLgth');
	const orderChoice = document.querySelectorAll('.odcc');
	
	let checkboxesChecked = [];
	let stayDuration = [];
	let position = [];
	for (let i=0; i<checkboxes.length; i++) {
		if (checkboxes[i].checked) {
			checkboxesChecked.push(checkboxes[i]);
			stayDuration.push(duration[i].value);
			position.push(orderChoice[i].value);
		}
	}
	
	
}

*/